#!/usr/bin/env python3
'''A type-annotated function that finds the
floor of a float and returns it as an int.'''

import math


def floor(a: float) -> int:
    '''Returns the floor of float a as an int'''
    return math.floor(a)
